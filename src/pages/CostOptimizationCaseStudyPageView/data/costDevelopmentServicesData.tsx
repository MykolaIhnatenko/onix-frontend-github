import costDevelopmentServicesContentData from './costDevelopmentServicesContentData';

const costDevelopmentServicesData = {
  pageTitle: 'Initially, four separate RDS instances were used for each of the databases.',
  pageText: `Each site/app having its own large instance entailed expenses. To reduce the
        consumption of AWS resources, we made the following changes to the infrastructure:`,
  data: costDevelopmentServicesContentData,
};

export default costDevelopmentServicesData;
