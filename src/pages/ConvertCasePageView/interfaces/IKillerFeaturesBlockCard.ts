import { StaticImageData } from 'next/image';
import { ReactNode } from 'react';

interface IKillerFeaturesBlockCard {
  text: ReactNode;
  title: ReactNode;
  img: StaticImageData;
  background: StaticImageData;
}
export default IKillerFeaturesBlockCard;
