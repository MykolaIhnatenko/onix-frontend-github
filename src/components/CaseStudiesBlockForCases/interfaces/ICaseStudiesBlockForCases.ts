interface ICaseStudiesBlockForCases {
  sliderIds: number[];
  title?: string;
  saleUrl?: boolean;
  classes?: {
    section?: string;
    container?: string;
    title?: string;
  };
}

export default ICaseStudiesBlockForCases;
