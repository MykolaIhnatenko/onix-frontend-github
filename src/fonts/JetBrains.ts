import { JetBrains_Mono } from 'next/font/google';

const jetBrains = JetBrains_Mono({
  subsets: ['latin'],
  weight: ['500'],
  variable: '--jetBrains',
  display: 'swap',
});

export default jetBrains;
