import { StaticImageData } from 'next/image';

interface ICostOptimizationMainScreen {
  pageTitle: string;
  pageText: string;
  backgroundImg: StaticImageData;
  rightImg: StaticImageData;
}

export default ICostOptimizationMainScreen;
